# ميزة تتبع الدفعات في أوامر الشراء

## الوصف
تم إضافة نظام كامل لتتبع الدفعات في أوامر الشراء، يسمح بالدفع الفوري (كاش) أو الدفع بالتقسيط (آجل).

## المميزات المضافة

### 1. نوع الدفع في أمر الشراء
- **كاش (Cash)**: دفع فوري كامل عند إنشاء الأمر
- **آجل (Credit)**: دفع بالتقسيط على دفعات

### 2. تتبع حالة الدفع
- **pending**: لم يتم الدفع
- **partial**: دفع جزئي
- **paid**: مدفوع بالكامل

### 3. جدول الدفعات (purchase_order_payments)
جدول جديد لتسجيل كل دفعة:
- رقم الدفعة (payment_number)
- تاريخ الدفعة (payment_date)
- المبلغ (amount)
- طريقة الدفع (payment_method): كاش، تحويل بنكي، شيك
- رقم المرجع (reference_number)
- ملاحظات (notes)

### 4. التحديث التلقائي
عند إضافة أو تعديل أو حذف دفعة، يتم تحديث:
- المبلغ المدفوع (paid_amount)
- المبلغ المتبقي (remaining_amount)
- حالة الدفع (payment_status)

## التغييرات في قاعدة البيانات

### الحقول المضافة لجدول purchase_orders:
```sql
- payment_type: VARCHAR(20) DEFAULT 'cash' -- نوع الدفع
- paid_amount: NUMERIC(10, 2) DEFAULT 0 -- المبلغ المدفوع
- remaining_amount: NUMERIC(10, 2) DEFAULT 0 -- المبلغ المتبقي
- payment_status: VARCHAR(20) DEFAULT 'pending' -- حالة الدفع
```

### الجدول الجديد purchase_order_payments:
```sql
CREATE TABLE purchase_order_payments (
  id UUID PRIMARY KEY,
  order_id UUID REFERENCES purchase_orders(id),
  payment_number VARCHAR(50) UNIQUE,
  payment_date DATE,
  amount NUMERIC(10, 2),
  payment_method VARCHAR(50),
  reference_number VARCHAR(100),
  notes TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);
```

## التغييرات في الواجهة

### 1. صفحة إنشاء أمر شراء جديد (PurchaseOrderForm)
- إضافة حقل "طريقة الدفع" مع خيارين:
  - كاش (دفع فوري)
  - آجل (دفع بالتقسيط)
- عند اختيار "كاش": يتم تسجيل الأمر كمدفوع بالكامل
- عند اختيار "آجل": يتم تسجيل الأمر كغير مدفوع

### 2. صفحة تفاصيل أمر الشراء (PurchaseOrderDetail)
- عرض نوع الدفع (كاش/آجل)
- عرض حالة الدفع (pending/partial/paid)
- عرض المبلغ المدفوع والمتبقي (للأوامر الآجلة)
- قسم "الدفعات" يظهر فقط للأوامر الآجلة
- جدول يعرض جميع الدفعات المسجلة
- زر "إضافة دفعة" لتسجيل دفعة جديدة

### 3. نافذة إضافة دفعة (Payment Modal)
- حقل المبلغ (مع التحقق من عدم تجاوز المبلغ المتبقي)
- حقل طريقة الدفع (كاش، تحويل بنكي، شيك)
- حقل رقم المرجع (اختياري)
- حقل الملاحظات (اختياري)

### 4. صفحة قائمة أوامر الشراء (PurchaseOrderList)
- إضافة عمود "حالة الدفع" في الجدول
- عرض Badge ملون حسب حالة الدفع:
  - أخضر: كاش أو مدفوع بالكامل
  - أزرق: دفع جزئي
  - أصفر: آجل (لم يتم الدفع)

## الصلاحيات (RLS Policies)

### عرض الدفعات:
- المستخدمون في نفس الفرع
- Admin, Purchase Manager, Warehouse Manager

### إضافة دفعات:
- Admin, Purchase Manager, Warehouse Manager

### تعديل الدفعات:
- Admin, Purchase Manager

### حذف الدفعات:
- Admin, Purchase Manager

## كيفية الاستخدام

### 1. إنشاء أمر شراء جديد:
1. اذهب إلى "أوامر الشراء" → "أمر جديد"
2. املأ بيانات الأمر (المورد، الفرع، الأصناف)
3. اختر "طريقة الدفع":
   - **كاش**: سيتم تسجيل الأمر كمدفوع بالكامل
   - **آجل**: سيتم تسجيل الأمر كغير مدفوع
4. احفظ الأمر

### 2. إضافة دفعة لأمر آجل:
1. افتح تفاصيل أمر الشراء
2. في قسم "الدفعات"، اضغط على "إضافة دفعة"
3. أدخل:
   - المبلغ (لا يمكن أن يتجاوز المبلغ المتبقي)
   - طريقة الدفع
   - رقم المرجع (إن وجد)
   - ملاحظات (اختياري)
4. احفظ الدفعة
5. سيتم تحديث حالة الدفع تلقائياً

### 3. متابعة حالة الدفع:
- في قائمة أوامر الشراء، يمكنك رؤية حالة الدفع لكل أمر
- في تفاصيل الأمر، يمكنك رؤية:
  - المبلغ الإجمالي
  - المبلغ المدفوع
  - المبلغ المتبقي
  - جميع الدفعات المسجلة

## الملفات المعدلة

### قاعدة البيانات:
- `database/migrations/add_payment_tracking_to_purchase_orders.sql`
- `database/migrations/add_rls_policies_for_purchase_order_payments.sql`
- `database/migrations/fix_payment_status_trigger_for_delete.sql`

### TypeScript Types:
- `restaurant-react/src/types/database.types.ts`

### Services:
- `restaurant-react/src/services/purchase.service.ts`

### Components:
- `restaurant-react/src/pages/purchase/PurchaseOrderForm.tsx`
- `restaurant-react/src/pages/purchase/PurchaseOrderDetail.tsx`
- `restaurant-react/src/pages/purchase/PurchaseOrderList.tsx`

## ملاحظات مهمة

1. **التحقق من المبلغ**: عند إضافة دفعة، يتم التحقق من أن المبلغ لا يتجاوز المبلغ المتبقي
2. **التحديث التلقائي**: يتم تحديث حالة الدفع تلقائياً عند إضافة أو تعديل أو حذف دفعة
3. **الأوامر الكاش**: لا يظهر قسم الدفعات للأوامر المدفوعة كاش
4. **الصلاحيات**: فقط المستخدمون المصرح لهم يمكنهم إضافة أو تعديل الدفعات

## أمثلة الاستخدام

### مثال 1: أمر شراء كاش
```
- نوع الدفع: كاش
- المبلغ الإجمالي: 10,000 ج.م
- المبلغ المدفوع: 10,000 ج.م
- المبلغ المتبقي: 0 ج.م
- حالة الدفع: مدفوع بالكامل
```

### مثال 2: أمر شراء آجل مع دفعات
```
- نوع الدفع: آجل
- المبلغ الإجمالي: 50,000 ج.م

الدفعة الأولى:
- المبلغ: 20,000 ج.م
- التاريخ: 2026-01-22
- حالة الدفع: دفع جزئي
- المتبقي: 30,000 ج.م

الدفعة الثانية:
- المبلغ: 15,000 ج.م
- التاريخ: 2026-02-22
- حالة الدفع: دفع جزئي
- المتبقي: 15,000 ج.م

الدفعة الثالثة:
- المبلغ: 15,000 ج.م
- التاريخ: 2026-03-22
- حالة الدفع: مدفوع بالكامل
- المتبقي: 0 ج.م
```

## التطويرات المستقبلية المقترحة

1. إضافة تقارير للدفعات المستحقة
2. إضافة تنبيهات للدفعات المتأخرة
3. إضافة إمكانية جدولة الدفعات مسبقاً
4. إضافة إمكانية طباعة إيصال الدفعة
5. إضافة تكامل مع أنظمة الدفع الإلكتروني
