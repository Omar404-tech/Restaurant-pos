# دليل استيراد الوصفات من Excel

## الميزة الجديدة ✨

تم إضافة ميزة استيراد الوصفات (Recipes) من ملف Excel/CSV لإضافة عدة وصفات مرة واحدة بدلاً من إدخالها يدوياً واحدة تلو الأخرى.

## كيفية الاستخدام

### 1. الوصول للصفحة
- اذهب إلى صفحة الوصفات: `/recipes`
- اضغط على زر "استيراد من Excel" (اللون البنفسجي)

### 2. تحميل ملف النموذج
- اضغط على زر "تحميل ملف النموذج"
- سيتم تحميل ملف CSV يحتوي على مثال للبيانات المطلوبة

### 3. تعبئة البيانات

#### تنسيق الملف:
```csv
اسم الوصفة,كود الصنف,اسم الصنف,الكمية,ملاحظات
بيتزا مارجريتا,ITEM001,دقيق,0.5,
بيتزا مارجريتا,ITEM002,جبنة موتزاريلا,0.2,
بيتزا مارجريتا,ITEM003,صلصة طماطم,0.1,
برجر لحم,ITEM004,لحم مفروم,0.15,
برجر لحم,ITEM005,خبز برجر,1,
```

#### القواعد المهمة:
1. **اسم الوصفة**: نفس الاسم للوصفة الواحدة في كل الصفوف
2. **كود الصنف**: يجب أن يكون موجوداً في النظام
3. **الكمية**: رقم عشري (مثال: 0.5, 1, 2.5)
4. **كل صف = مكون واحد** في الوصفة

### 4. رفع الملف
- اضغط على "اختر ملف CSV" أو اسحب الملف
- ستظهر معاينة لأول 5 صفوف

### 5. بدء الاستيراد
- اضغط على "بدء الاستيراد"
- انتظر حتى تكتمل العملية

### 6. النتائج
- سيظهر عدد الوصفات الناجحة والفاشلة
- إذا كانت هناك أخطاء، ستظهر تفاصيلها

## الأخطاء الشائعة

### 1. "الصنف غير موجود"
- **السبب**: كود الصنف غير موجود في قاعدة البيانات
- **الحل**: تأكد من إضافة الصنف أولاً أو استخدم كود صحيح

### 2. "بيانات غير كاملة"
- **السبب**: كود الصنف أو الكمية فارغة
- **الحل**: تأكد من ملء جميع الحقول المطلوبة

### 3. "لا توجد مكونات صالحة"
- **السبب**: جميع المكونات فشلت في التحقق
- **الحل**: راجع أكواد الأصناف والكميات

## مثال عملي

### وصفة بيتزا مارجريتا:
```csv
اسم الوصفة,كود الصنف,اسم الصنف,الكمية,ملاحظات
بيتزا مارجريتا,FLOUR001,دقيق,0.5,عجينة البيتزا
بيتزا مارجريتا,CHEESE001,جبنة موتزاريلا,0.2,
بيتزا مارجريتا,SAUCE001,صلصة طماطم,0.1,
بيتزا مارجريتا,OIL001,زيت زيتون,0.02,
```

### وصفة برجر لحم:
```csv
اسم الوصفة,كود الصنف,اسم الصنف,الكمية,ملاحظات
برجر لحم,MEAT001,لحم مفروم,0.15,
برجر لحم,BREAD001,خبز برجر,1,
برجر لحم,LETTUCE001,خس,0.05,
برجر لحم,TOMATO001,طماطم,0.05,
برجر لحم,CHEESE002,جبنة شيدر,0.03,
```

## ملاحظات مهمة

1. **الترميز**: احفظ الملف بترميز UTF-8 لدعم العربية
2. **التكرار**: الوصفات بنفس الاسم سيتم دمج مكوناتها
3. **التصنيف**: سيتم وضع الوصفات في تصنيف "Recipes" تلقائياً
4. **الكود**: سيتم توليد كود تلقائي لكل وصفة (RCP-timestamp)

## الملفات المعدلة

### ملفات جديدة:
- `restaurant-react/src/pages/recipes/RecipeImport.tsx` - صفحة الاستيراد

### ملفات محدثة:
- `restaurant-react/src/App.tsx` - إضافة route جديد
- `restaurant-react/src/pages/recipes/RecipeList.tsx` - إضافة زر الاستيراد
- `restaurant-react/src/services/recipe.service.ts` - إضافة دوال الاستيراد

## الدعم الفني

إذا واجهت أي مشاكل:
1. تأكد من أن جميع الأصناف موجودة في النظام
2. تحقق من تنسيق الملف (CSV بترميز UTF-8)
3. راجع رسائل الأخطاء في نتائج الاستيراد
