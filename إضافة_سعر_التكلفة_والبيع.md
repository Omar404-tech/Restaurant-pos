# ✅ تم إضافة سعر التكلفة وسعر البيع في إدارة نقاط البيع

## التاريخ: 25 يناير 2026

---

## 🎯 المطلوب

إضافة عمودين جديدين في صفحة "إدارة نقاط البيع":
1. **سعر التكلفة/الشراء** - من جدول items (purchase_price)
2. **سعر البيع** - السعر في نقطة البيع (price)
3. **الربح** - الفرق بين سعر البيع وسعر التكلفة
4. القدرة على تعديل كلا السعرين

---

## ✅ التنفيذ

### 1. تحديث Service ✅

تم إضافة:
- `purchase_price` في interface `BranchPOSItem`
- دالة جديدة `updateItemCost()` لتحديث سعر التكلفة

```typescript
// Update item purchase price (cost)
async updateItemCost(itemId: string, purchasePrice: number) {
  const { data, error } = await supabase
    .from('items')
    .update({
      purchase_price: purchasePrice,
      updated_at: new Date().toISOString()
    })
    .eq('id', itemId)
    .select()
    .single()

  return { data, error }
}
```

### 2. تحديث الواجهة ✅

تم إضافة الأعمدة التالية في الجدول:

| العمود | الوصف | قابل للتعديل |
|--------|-------|--------------|
| سعر التكلفة | من جدول items (purchase_price) | ✅ نعم |
| سعر البيع | من جدول branch_pos_items (price) | ✅ نعم |
| الربح | سعر البيع - سعر التكلفة | ❌ محسوب تلقائياً |

### 3. حساب الربح ✅

يتم حساب الربح تلقائياً:
```typescript
const profit = currentPrice - currentCost
const profitPercent = currentCost > 0 ? ((profit / currentCost) * 100) : 0
```

**الألوان:**
- 🟢 أخضر: ربح موجب
- 🔴 أحمر: خسارة (ربح سالب)

---

## 🎨 الشكل النهائي

### الجدول الآن يحتوي على:

```
┌──────┬────────┬──────┬────────┬──────────┬────────────┬──────────┬────────┬────────┬────────┐
│ الكود │ المنتج │ النوع│ الوحدة │ الكمية   │ سعر التكلفة│ سعر البيع│ الربح  │ الحالة │ إجراءات│
├──────┼────────┼──────┼────────┼──────────┼────────────┼──────────┼────────┼────────┼────────┤
│ CH001│ زنجر  │ ريسبي│  قطعة  │   50.00  │   [25.00]  │  [50.00] │ 25.00  │   👁️   │   🗑️   │
│      │        │      │        │          │            │          │  100%  │        │        │
└──────┴────────┴────────┴────────┴──────────┴────────────┴──────────┴────────┴────────┴────────┘
```

**ملاحظات:**
- `[25.00]` = حقل قابل للتعديل
- `25.00` = قيمة محسوبة تلقائياً
- `100%` = نسبة الربح المئوية

---

## 📊 مثال عملي

### مثال 1: زنجر
- **سعر التكلفة**: 25 جنيه
- **سعر البيع**: 50 جنيه
- **الربح**: 25 جنيه (100%)
- **اللون**: 🟢 أخضر

### مثال 2: بيبسي
- **سعر التكلفة**: 10 جنيه
- **سعر البيع**: 15 جنيه
- **الربح**: 5 جنيه (50%)
- **اللون**: 🟢 أخضر

### مثال 3: عرض خاص (خسارة)
- **سعر التكلفة**: 30 جنيه
- **سعر البيع**: 25 جنيه
- **الربح**: -5 جنيه (-16.7%)
- **اللون**: 🔴 أحمر

---

## 🚀 كيفية الاستخدام

### 1. فتح إدارة نقاط البيع
```
القائمة الجانبية → إدارة نقاط البيع
```

### 2. اختيار الفرع
اختر الفرع الذي تريد إدارة أسعاره

### 3. تعديل الأسعار

#### تعديل سعر التكلفة:
1. اضغط على حقل "سعر التكلفة"
2. أدخل السعر الجديد
3. سيتم تحديث الربح تلقائياً

#### تعديل سعر البيع:
1. اضغط على حقل "سعر البيع"
2. أدخل السعر الجديد
3. سيتم تحديث الربح تلقائياً

### 4. حفظ التغييرات
اضغط زر "حفظ التغييرات" في الأعلى

---

## 💡 ميزات إضافية

### 1. تمييز التعديلات
- الصفوف المعدلة تظهر بخلفية صفراء فاتحة
- يسهل معرفة ما تم تعديله قبل الحفظ

### 2. حساب تلقائي للربح
- يتم حساب الربح فوراً عند تغيير أي سعر
- يظهر الربح بالجنيه والنسبة المئوية

### 3. ألوان تحذيرية
- 🟢 أخضر: ربح جيد
- 🔴 أحمر: خسارة (تحذير!)

### 4. تعديل مستقل
- سعر التكلفة يُحفظ في جدول `items` (عام لكل الفروع)
- سعر البيع يُحفظ في جدول `branch_pos_items` (خاص بكل فرع)

---

## 📝 ملاحظات مهمة

### سعر التكلفة (Purchase Price)
- يتم حفظه في جدول `items`
- **مشترك بين جميع الفروع**
- عند تعديله، يؤثر على جميع الفروع
- يُستخدم لحساب الربح

### سعر البيع (Selling Price)
- يتم حفظه في جدول `branch_pos_items`
- **مستقل لكل فرع**
- كل فرع يمكنه تحديد سعر بيع مختلف
- هو السعر الذي يظهر للكاشير

### الربح (Profit)
- **محسوب تلقائياً** = سعر البيع - سعر التكلفة
- **نسبة الربح** = (الربح ÷ سعر التكلفة) × 100
- لا يُحفظ في قاعدة البيانات (يُحسب عند العرض)

---

## 🧪 اختبار الميزة

### سيناريو الاختبار:

1. **تسجيل الدخول** كـ admin@restaurant.com
2. **فتح** "إدارة نقاط البيع"
3. **اختيار** "الفرع 1"
4. **تعديل سعر التكلفة** لمنتج (مثلاً: 25 جنيه)
5. **تعديل سعر البيع** لنفس المنتج (مثلاً: 50 جنيه)
6. **مشاهدة** الربح يتحدث تلقائياً (25 جنيه، 100%)
7. **حفظ** التغييرات
8. **تحديث** الصفحة والتحقق من حفظ الأسعار

---

## 📊 التقارير المستقبلية

يمكن الآن إنشاء تقارير:
- تقرير الربحية لكل منتج
- تقرير المنتجات الأكثر ربحاً
- تقرير المنتجات ذات الخسارة
- مقارنة الأسعار بين الفروع

---

## 🔧 التعديلات التقنية

### الملفات المعدلة:

1. ✅ `restaurant-react/src/services/posManagement.service.ts`
   - إضافة `purchase_price` في interface
   - إضافة دالة `updateItemCost()`

2. ✅ `restaurant-react/src/pages/pos/POSManagement.tsx`
   - إضافة state `editedCosts`
   - إضافة دالة `handleCostChange()`
   - إضافة أعمدة جديدة في الجدول
   - تحديث دالة `handleSave()`

---

## ✅ الخلاصة

**تم إضافة:**
- ✅ عمود سعر التكلفة (قابل للتعديل)
- ✅ عمود سعر البيع (قابل للتعديل)
- ✅ عمود الربح (محسوب تلقائياً)
- ✅ نسبة الربح المئوية
- ✅ ألوان تحذيرية للربح/الخسارة
- ✅ حفظ التعديلات في قاعدة البيانات

**الحالة:** ✅ **جاهز للاستخدام!**

---

**🎉 الميزة جاهزة! يمكنك الآن إدارة أسعار التكلفة والبيع بسهولة! 🎉**

التاريخ: 25 يناير 2026
