# تفعيل دعم الكسور العشرية في الكميات 🔢

## الخبر السار ✅

**قاعدة البيانات جاهزة بالفعل!** 🎉

النظام يدعم الكسور العشرية في قاعدة البيانات، ويمكنك تخزين قيم مثل:
- 19.200 كجم لحمة
- 5.750 لتر زيت
- 100.125 كجم دقيق

---

## ما تحتاج عمله 🛠️

### 1. تحديث الواجهة الأمامية (React)

يجب تحديث حقول إدخال الكميات لتقبل الكسور.

#### الطريقة السريعة (موصى بها):

```bash
# افتح Terminal في مجلد المشروع
cd restaurant-react

# شغل السكريبت التلقائي
node ../scripts/update_quantity_inputs.js
```

هذا السكريبت سيحدث تلقائياً جميع حقول الكميات في:
- ✅ المخزون
- ✅ التحويلات
- ✅ التوريدات
- ✅ المرتجعات
- ✅ طلبات الشراء
- ✅ التلفيات
- ✅ الوصفات

---

### 2. اختبار التحديثات

بعد تشغيل السكريبت، جرب:

1. افتح أي نموذج (مثل: إضافة توريد)
2. جرب إدخال كمية بكسور: `19.200`
3. تأكد أن النظام يقبل الكمية
4. احفظ واتأكد أن البيانات تُحفظ صح

---

### 3. استخدام الدوال المساعدة (اختياري)

تم إضافة دوال مساعدة في `restaurant-react/src/lib/utils.ts`:

```typescript
import { formatQuantity, parseQuantity, isValidQuantity } from '@/lib/utils';

// لتنسيق الكميات (إزالة الأصفار الزائدة)
formatQuantity(19.200)  // => "19.2"
formatQuantity(5.000)   // => "5"

// لتحليل الكمية من النص
parseQuantity("19.2")   // => 19.2
parseQuantity("19,2")   // => 19.2 (يدعم الفاصلة العربية!)

// للتحقق من صحة الكمية
isValidQuantity(19.2)   // => true
isValidQuantity(0)      // => false
isValidQuantity(-5)     // => false
```

---

## التغييرات التي تمت 📝

### قبل التحديث ❌
```tsx
<input 
  type="number" 
  min="1"           // ❌ يمنع الكسور
  value={quantity}
/>
```

### بعد التحديث ✅
```tsx
<input 
  type="number" 
  min="0.001"       // ✅ يسمح بالكسور
  step="0.001"      // ✅ يسمح بـ 3 أرقام عشرية
  value={quantity}
  placeholder="مثال: 19.200"
/>
```

---

## أمثلة الاستخدام 💡

### مثال 1: توريد لحمة
```
الصنف: لحمة بقري
الكمية: 19.200 كجم
السعر: 250 جنيه/كجم
الإجمالي: 4,800 جنيه
```

### مثال 2: تحويل زيت
```
من: المخزن الرئيسي
إلى: فرع المعادي
الصنف: زيت ذرة
الكمية: 5.750 لتر
```

### مثال 3: جرد يومي
```
الصنف: دقيق
الكمية في النظام: 100.000 كجم
الكمية الفعلية: 98.750 كجم
الفرق: -1.250 كجم
```

---

## الملفات المهمة 📁

1. **`DECIMAL_QUANTITIES_README.md`** - دليل شامل بالعربي والإنجليزي
2. **`DECIMAL_QUANTITIES_IMPLEMENTATION.md`** - تفاصيل تقنية كاملة
3. **`database/enable_decimal_quantities.sql`** - سكريبت قاعدة البيانات
4. **`scripts/update_quantity_inputs.js`** - سكريبت التحديث التلقائي
5. **`restaurant-react/src/lib/utils.ts`** - الدوال المساعدة

---

## استكشاف الأخطاء 🔍

### المشكلة: لا يمكن إدخال كسور
**الحل:** تأكد أنك شغلت السكريبت التلقائي أو أضفت `step="0.001"` يدوياً

### المشكلة: الكمية تُحفظ كرقم صحيح
**الحل:** قاعدة البيانات صح، المشكلة في الواجهة - شغل السكريبت

### المشكلة: الأصفار الزائدة تظهر (19.200 بدل 19.2)
**الحل:** استخدم `formatQuantity()` عند عرض الكميات

---

## خطوات سريعة للتنفيذ ⚡

```bash
# 1. تحديث الواجهة
cd restaurant-react
node ../scripts/update_quantity_inputs.js

# 2. اختبار قاعدة البيانات (اختياري)
psql -U your_user -d your_database -f database/enable_decimal_quantities.sql

# 3. تشغيل التطبيق
npm run dev

# 4. اختبار إدخال كميات بكسور
# افتح أي نموذج وجرب: 19.200
```

---

## ملاحظات مهمة ⚠️

1. **الدقة**: النظام يدعم حتى 3 أرقام بعد الفاصلة (0.001)
2. **الحد الأدنى**: لا يمكن إدخال صفر أو قيم سالبة
3. **الحد الأقصى**: 999,999,999.999
4. **الفاصلة العربية**: مدعومة! يمكنك كتابة 19,2 أو 19.2

---

## الدعم 📞

إذا واجهت أي مشكلة:
1. راجع `DECIMAL_QUANTITIES_README.md` للتفاصيل
2. تحقق من الملفات المحدثة
3. تأكد أن السكريبت اشتغل صح

---

**الحالة**: جاهز للتنفيذ ✅  
**الوقت المتوقع**: 5-10 دقائق  
**الصعوبة**: سهل 🟢

---

## بعد التنفيذ ✨

بعد ما تخلص، هتقدر:
- ✅ تدخل كميات بكسور في كل النماذج
- ✅ تخزن البيانات بدقة عالية
- ✅ تعرض الكميات بشكل منسق
- ✅ تحسب الإجماليات بدقة

**يلا نبدأ! 🚀**
