# ✅ حساب تكلفة الريسبي تلقائياً من المكونات

## التاريخ: 25 يناير 2026

---

## 🎯 الفكرة

**سعر التكلفة يتم حسابه بطريقتين:**

### 1. المنتجات العادية (Products)
- سعر التكلفة = `purchase_price` من جدول items
- **قابل للتعديل يدوياً** ✏️
- مثال: دجاج، بصل، طماطم، بيبسي

### 2. الريسبيات (Recipes)
- سعر التكلفة = **مجموع تكلفة المكونات** 🧮
- **محسوب تلقائياً** (غير قابل للتعديل)
- مثال: زنجر، برجر، بيتزا

---

## 📐 معادلة حساب تكلفة الريسبي

```
تكلفة الريسبي = Σ (كمية المكون × سعر شراء المكون)
```

### مثال عملي: زنجر

| المكون | الكمية | سعر الشراء | التكلفة |
|--------|--------|------------|---------|
| دجاج | 0.25 كجم | 80 ج/كجم | 20 ج |
| دقيق | 0.05 كجم | 30 ج/كجم | 1.5 ج |
| زيت | 0.1 لتر | 45 ج/لتر | 4.5 ج |
| بهارات | 0.02 كجم | 200 ج/كجم | 4 ج |
| **المجموع** | - | - | **30 ج** |

**إذن تكلفة الزنجر = 30 جنيه** 🧮

---

## ✅ التنفيذ

### 1. Database Function ✅

تم إنشاء دالة `calculate_recipe_cost()`:

```sql
CREATE OR REPLACE FUNCTION calculate_recipe_cost(recipe_item_id uuid)
RETURNS numeric AS $$
DECLARE
    total_cost numeric := 0;
    ingredient_record RECORD;
BEGIN
    -- Sum up the cost of all ingredients
    FOR ingredient_record IN
        SELECT 
            ri.quantity,
            i.purchase_price
        FROM recipe_ingredients ri
        INNER JOIN items i ON ri.ingredient_item_id = i.id
        WHERE ri.recipe_item_id = calculate_recipe_cost.recipe_item_id
    LOOP
        total_cost := total_cost + (ingredient_record.quantity * COALESCE(ingredient_record.purchase_price, 0));
    END LOOP;
    
    RETURN total_cost;
END;
$$ LANGUAGE plpgsql;
```

### 2. Database View ✅

تم إنشاء view `v_items_with_calculated_cost`:

```sql
CREATE OR REPLACE VIEW v_items_with_calculated_cost AS
SELECT 
    i.*,
    CASE 
        WHEN i.is_recipe THEN calculate_recipe_cost(i.id)
        ELSE i.purchase_price
    END as calculated_cost
FROM items i
WHERE i.status = 'active';
```

### 3. Frontend Service ✅

تم تحديث `posManagementService.getBranchPOSItems()`:
- يستدعي `calculate_recipe_cost()` للريسبيات
- يستخدم `purchase_price` للمنتجات العادية
- يضيف `calculated_cost` لكل منتج

### 4. Frontend UI ✅

تم تحديث `POSManagement.tsx`:
- حقل سعر التكلفة **معطل** للريسبيات (خلفية بنفسجية)
- حقل سعر التكلفة **قابل للتعديل** للمنتجات العادية
- أيقونة 🧮 بجانب الريسبيات للدلالة على الحساب التلقائي

---

## 🎨 الشكل النهائي

### الجدول الآن:

```
┌──────┬────────┬──────┬────────┬──────────┬────────────────┬──────────┬────────┬────────┬────────┐
│ الكود │ المنتج │ النوع│ الوحدة │ الكمية   │ سعر التكلفة    │ سعر البيع│ الربح  │ الحالة │ إجراءات│
├──────┼────────┼──────┼────────┼──────────┼────────────────┼──────────┼────────┼────────┼────────┤
│RCP001│ زنجر  │ ريسبي│  قطعة  │   50.00  │  [44.00] 🧮    │  [80.00] │ 36.00  │   👁️   │   🗑️   │
│      │        │      │        │          │  (محسوب)       │          │  81.8% │        │        │
├──────┼────────┼──────┼────────┼──────────┼────────────────┼──────────┼────────┼────────┼────────┤
│ITM001│ دجاج   │ منتج │   كجم  │  100.00  │  [80.00] ✏️    │ [120.00] │ 40.00  │   👁️   │   🗑️   │
│      │        │      │        │          │  (يدوي)        │          │  50.0% │        │        │
└──────┴────────┴────────┴────────┴──────────┴────────────────┴──────────┴────────┴────────┴────────┘
```

**الرموز:**
- 🧮 = محسوب تلقائياً من المكونات
- ✏️ = قابل للتعديل يدوياً
- [44.00] = حقل معطل (خلفية بنفسجية)
- [80.00] = حقل قابل للتعديل

---

## 💡 الميزات

### 1. حساب تلقائي ✅
- الريسبيات تُحسب تكلفتها تلقائياً
- عند تغيير سعر أي مكون، تتحدث تكلفة الريسبي تلقائياً
- لا حاجة لإدخال يدوي

### 2. تمييز بصري ✅
- **خلفية بنفسجية** للريسبيات (معطلة)
- **خلفية بيضاء** للمنتجات العادية (قابلة للتعديل)
- **أيقونة 🧮** بجانب الريسبيات

### 3. دقة الحساب ✅
- يأخذ في الاعتبار كمية كل مكون
- يستخدم أحدث أسعار الشراء
- يحسب بدقة عشرية

### 4. حساب الربح الصحيح ✅
- الربح = سعر البيع - التكلفة المحسوبة
- نسبة الربح = (الربح ÷ التكلفة) × 100

---

## 📊 أمثلة عملية

### مثال 1: ريسبي زنجر

**المكونات:**
```
دجاج:   0.25 كجم × 80 ج  = 20.00 ج
دقيق:   0.05 كجم × 30 ج  =  1.50 ج
زيت:    0.10 لتر × 45 ج  =  4.50 ج
بهارات: 0.02 كجم × 200 ج =  4.00 ج
جبن:    0.05 كجم × 120 ج =  6.00 ج
خبز:    1.00 قطعة × 2 ج  =  2.00 ج
خس:     0.02 كجم × 20 ج  =  0.40 ج
طماطم:  0.05 كجم × 15 ج  =  0.75 ج
صوص:    0.03 لتر × 50 ج  =  1.50 ج
─────────────────────────────────────
المجموع:                    40.65 ج
```

**التسعير:**
- سعر التكلفة: **40.65 ج** (محسوب تلقائياً)
- سعر البيع: **80 ج** (يدوي)
- الربح: **39.35 ج** (98%)

### مثال 2: منتج عادي (دجاج)

**التسعير:**
- سعر التكلفة: **80 ج/كجم** (يدوي)
- سعر البيع: **120 ج/كجم** (يدوي)
- الربح: **40 ج** (50%)

---

## 🔄 التحديث التلقائي

### عند تغيير سعر مكون:

**قبل:**
```
دجاج: 80 ج/كجم
زنجر (يستخدم 0.25 كجم دجاج): تكلفة = 40 ج
```

**بعد تغيير سعر الدجاج إلى 100 ج:**
```
دجاج: 100 ج/كجم
زنجر (يستخدم 0.25 كجم دجاج): تكلفة = 45 ج ✅ (تحدث تلقائياً!)
```

**النتيجة:**
- تكلفة الزنجر زادت من 40 ج إلى 45 ج
- الربح انخفض من 40 ج إلى 35 ج
- نسبة الربح انخفضت من 100% إلى 77.8%

---

## 🚀 كيفية الاستخدام

### 1. للمنتجات العادية:
1. افتح "إدارة نقاط البيع"
2. اختر الفرع
3. عدّل سعر التكلفة يدوياً ✏️
4. احفظ التغييرات

### 2. للريسبيات:
1. افتح "إدارة نقاط البيع"
2. اختر الفرع
3. **لا يمكن تعديل سعر التكلفة** (محسوب تلقائياً) 🧮
4. عدّل سعر البيع فقط
5. احفظ التغييرات

### 3. لتغيير تكلفة ريسبي:
**يجب تغيير أسعار المكونات:**
1. اذهب إلى "المخزون" → "الأصناف"
2. عدّل سعر الشراء للمكون
3. ارجع لـ "إدارة نقاط البيع"
4. ستجد تكلفة الريسبي تحدثت تلقائياً ✅

---

## 📝 ملاحظات مهمة

### 1. دقة الحساب
- يتم الحساب بدقة عشرية (decimal)
- يأخذ في الاعتبار الكميات الصغيرة (0.05 كجم مثلاً)

### 2. تحديث الأسعار
- عند تغيير سعر مكون، تتحدث تكلفة جميع الريسبيات التي تستخدمه
- التحديث فوري عند فتح صفحة إدارة نقاط البيع

### 3. المكونات المفقودة
- إذا كان مكون بدون سعر شراء، يُحسب بـ 0
- تأكد من إدخال أسعار جميع المكونات

### 4. الريسبيات المعقدة
- يمكن للريسبي أن يحتوي على ريسبي آخر كمكون
- الحساب يتم بشكل متداخل (recursive)

---

## 🧪 اختبار الميزة

### سيناريو الاختبار:

1. **إنشاء ريسبي جديد:**
   - اذهب لـ "الريسبيات"
   - أنشئ ريسبي "برجر"
   - أضف مكونات: لحم (0.2 كجم)، خبز (1 قطعة)، جبن (0.05 كجم)

2. **تحديد أسعار المكونات:**
   - لحم: 250 ج/كجم
   - خبز: 2 ج/قطعة
   - جبن: 120 ج/كجم

3. **الحساب المتوقع:**
   ```
   لحم:  0.2 × 250 = 50.00 ج
   خبز:  1.0 × 2   =  2.00 ج
   جبن:  0.05 × 120 = 6.00 ج
   ─────────────────────────
   المجموع:           58.00 ج
   ```

4. **التحقق:**
   - افتح "إدارة نقاط البيع"
   - أضف البرجر لنقطة البيع
   - تحقق أن سعر التكلفة = **58 ج** ✅

5. **تغيير سعر مكون:**
   - غيّر سعر اللحم إلى 300 ج/كجم
   - ارجع لـ "إدارة نقاط البيع"
   - تحقق أن سعر التكلفة = **68 ج** ✅ (زاد 10 ج)

---

## 🔧 التعديلات التقنية

### الملفات المعدلة:

1. ✅ **Database**
   - إنشاء function `calculate_recipe_cost()`
   - إنشاء view `v_items_with_calculated_cost`

2. ✅ **Backend Service**
   - `posManagement.service.ts`
   - إضافة `calculated_cost` في interface
   - استدعاء `calculate_recipe_cost()` للريسبيات

3. ✅ **Frontend UI**
   - `POSManagement.tsx`
   - تعطيل حقل التكلفة للريسبيات
   - إضافة أيقونة 🧮
   - خلفية بنفسجية للحقول المعطلة

---

## ✅ الخلاصة

**تم تنفيذ:**
- ✅ حساب تلقائي لتكلفة الريسبيات من المكونات
- ✅ تعطيل تعديل تكلفة الريسبيات (محسوبة تلقائياً)
- ✅ إمكانية تعديل تكلفة المنتجات العادية
- ✅ تمييز بصري بين الريسبيات والمنتجات
- ✅ حساب دقيق للربح بناءً على التكلفة الحقيقية
- ✅ تحديث تلقائي عند تغيير أسعار المكونات

**الحالة:** ✅ **جاهز للاستخدام!**

---

**🎉 الآن يمكنك معرفة التكلفة الحقيقية لكل ريسبي والربح الفعلي! 🎉**

التاريخ: 25 يناير 2026
